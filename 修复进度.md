# ExtraSoundsNext 项目修复进度

> 最后更新：2025年2月12日
> 状态：✅ 所有 Fabric 版本修复完成 (5/5)

## 项目概述

**ExtraSounds Next** 是一个使用 Stonecutter 构建的 Minecraft 多版本模组，支持：
- Minecraft 1.18.2 - 1.21.1
- Fabric 和 NeoForge/Forge 加载器

## 当前状态

### ✅ 可以成功编译的版本 (9/11) - 82%
- `1.18.2-fabric` ✅
- `1.19.2-fabric` ✅
- `1.19.4-fabric` ✅
- `1.20.1-fabric` ✅
- `1.21.1-fabric` ✅
- `1.21.1-neoforge` ✅
- `1.18.2-forge` ✅ (2026年2月12日修复)
- `1.19.2-forge` ✅ (2026年2月12日修复)
- `1.19.4-forge` ✅ (2026年2月12日修复)

### ⚠️ 需要修复的版本 (1/11)
- `1.20.1-forge` - ChatHudMixin 方法签名问题

## 已修复的问题

### 版本条件修复

以下文件已修复版本兼容性问题：

#### 1. CreativeInventoryScreenMixin.java
**问题**: `CreativeModeTabs.INVENTORY` 在不同版本的访问方式不同
```java
//? if >=1.20 {
private static final CreativeModeTab GROUP_INVENTORY = BuiltInRegistries.CREATIVE_MODE_TAB.get(CreativeModeTabs.INVENTORY);
//?} else {
/*private static final CreativeModeTab GROUP_INVENTORY = CreativeModeTabs.INVENTORY;
*///?}
```

#### 2. SoundManager.java & SoundPackLoader.java
**问题**: `Registry.ITEM` 在 1.20+ 改为 `BuiltInRegistries.ITEM`
```java
//? if >=1.20 {
var itemId = BuiltInRegistries.ITEM.getKey(stack.getItem());
//?} elif >=1.19.3 {
/*var itemId = BuiltInRegistries.ITEM.getKey(stack.getItem());
//?} else {
/*var itemId = Registry.ITEM.getKey(stack.getItem());
*///?}
```

#### 3. VolumeScreen.java
**问题**: `GuiGraphics` 在 1.20+ 才存在，之前版本使用 `PoseStack`
```java
//? if >=1.20 {
import net.minecraft.client.gui.GuiGraphics;
//?} else {
/*import com.mojang.blaze3d.vertex.PoseStack;
*///?}
```

#### 4. VolumeSlider.java
**问题**: `Component.empty()` 和 `Component.translatable()` 版本差异
```java
//? if >=1.19 {
super(x, y, width, height, Component.empty(), VolumeConfig.getVolume(category));
//?} else {
/*super(x, y, width, height, net.minecraft.network.chat.TextComponent.EMPTY, VolumeConfig.getVolume(category));
*///?}
```

#### 5. SoundSettingsMixin.java
**问题**: Button 构造方法版本差异
```java
//?} else {
/*this.addRenderableWidget(
        new Button(this.width / 2 + 105, this.height - 27, 20, 20,
                Component.literal("⚙"), button -> {
            if (this.minecraft != null) {
                this.minecraft.setScreen(new VolumeScreen(this));
            }
        })
);
*///?}
```

#### 6. SoundSerializer.java
**问题**: `getVolume().sample()` 在不同版本的签名不同
```java
//? if >=1.19.3 {
if (src.getVolume() instanceof ConstantFloat volumeFloat && volumeFloat.getValue() != 1)
    o.addProperty("volume", src.getVolume().sample(r));
//?} else {
/*float volume = src.getVolume().sample(r);
if (volume != 1)
    o.addProperty("volume", volume);
*///?}
```

#### 7. ExtraSoundsNext.java
**问题**: `SoundEvent` 构造方法在不同版本不同
```java
//? if >=1.21 {
/*public static final SoundEvent MISSING = SoundEvent.createVariableRangeEvent(ExHelper.rl(MODID, "missing"));
//?} elif >=1.19.3 {
public static final SoundEvent MISSING = SoundEvent.createVariableRangeEvent(ExHelper.id("missing"));
//?} else {
/*public static final SoundEvent MISSING = new SoundEvent(ExHelper.rl("missing"));
*///?}
```

#### 8. DefaultAutoGenerator.java
**问题**: `SoundType.FROGLIGHT` 和某些 Item 类在旧版本不存在
```java
//? if >=1.19 {
/*} else if (block instanceof RotatedPillarBlock pillarBlock && SoundGenerator.getSoundType(pillarBlock).equals(SoundType.FROGLIGHT)) {
    return SoundDefinition.of(event(blockSound, 0.3f));
*///?}
```

#### 9. ChatHudMixin.java
**问题**: `addMessage` 方法签名在不同版本不同
```java
//? if >=1.20 {
/*@Inject(method = "addMessage(Lnet/minecraft/network/chat/Component;Lnet/minecraft/network/chat/MessageSignature;Lnet/minecraft/client/GuiMessageTag;IZ)V", at = @At("RETURN"))
//?} elif >=1.19 {
/*@Inject(method = "addMessage(Lnet/minecraft/network/chat/Component;Lnet/minecraft/network/chat/MessageSignature;ILnet/minecraft/client/GuiMessageTag;Z)V", at = @At("RETURN"))
//?} else {
/*@Inject(method = "addMessage(Lnet/minecraft/network/chat/Component;IIZ)V", at = @At("RETURN"))
//?}
```

## 仍需修复的关键问题

### 1.18.2 版本问题

#### RandomSource 类不存在
```java
// 旧版本使用
Random r = new Random();

// 1.19+ 使用
RandomSource r = RandomSource.create();
```

#### Component API 完全不同
```java
// 1.18.2 使用
new TextComponent("text")
new TranslatableComponent("key")

// 1.19+ 使用
Component.literal("text")
Component.translatable("key")
```

#### Button 构造方法
```java
// 1.18.2/1.19.2 使用旧 API
new Button(x, y, width, height, text, onPress)

// 1.19.4+ 使用 builder API
Button.builder(text, onPress).bounds(x, y, width, height).build()
```

#### 其他不存在的类
- `GuiMessageTag` - 1.19+ 才引入
- `MessageSignature` - 1.19+ 才引入
- `TextFieldHelper.CursorStep` - 1.19.3+ 才引入
- `ProfileKeyPair` - 1.19.3+ 才引入（但在 1.20+ 移除）

### 1.19.2 版本问题

#### Button.builder 不存在
- 1.19.2 仍使用旧的 `new Button()` 构造方法

#### Tooltip 类不存在
- 1.19.4+ 才有 `net.minecraft.client.gui.components.Tooltip`

### 1.19.4 版本问题

#### GuiGraphics 不存在
- 应使用 `PoseStack` 代替

#### BuiltInRegistries
- 1.19.3-1.19.4 使用 `BuiltInRegistries`
- 1.20+ 使用 `BuiltInRegistries`
- 需要区分

## 版本兼容性速查表

| API/类 | 1.18.2 | 1.19.2 | 1.19.4 | 1.20+ | 1.21+ |
|--------|--------|--------|--------|-------|-------|
| `Component.literal()` | ❌ | ✅ | ✅ | ✅ | ✅ |
| `Component.translatable()` | ❌ | ✅ | ✅ | ✅ | ✅ |
| `TextComponent` | ✅ | ❌ | ❌ | ❌ | ❌ |
| `Button.builder()` | ❌ | ❌ | ❌ | ✅ | ✅ |
| `GuiGraphics` | ❌ | ❌ | ❌ | ✅ | ✅ |
| `PoseStack` | ✅ | ✅ | ✅ | ❌ | ❌ |
| `BuiltInRegistries` | ❌ | ❌ | ✅* | ✅ | ✅ |
| `Registry.ITEM` | ✅ | ✅ | ❌ | ❌ | ❌ |
| `GuiMessageTag` | ❌ | ✅ | ✅ | ✅ | ✅ |
| `ProfileKeyPair` | ❌ | ❌ | ✅ | ❌ | ❌ |

*注：1.19.3-1.19.4 使用 `BuiltInRegistries`，但访问方式与 1.20+ 略有不同

## 关键文件清单

### 需要版本适配的核心文件

```
src/main/java/dev/arbor/extrasoundsnext/
├── ExtraSoundsNext.java                    # 平台抽象、SoundEvent 创建
├── mixin/
│   ├── chat/ChatHudMixin.java            # 聊天消息音效
│   ├── core/ClientPlayerEntityMixin.java  # 玩家动作音效
│   ├── gui/SoundSettingsMixin.java        # 音效设置按钮
│   ├── inventory/
│   │   ├── CreativeInventoryScreenMixin.java  # 创造物品栏音效
│   │   └── HandledScreenMixin.java
│   └── typing/SelectionManagerMixin.java  # 文本选择音效
├── sounds/
│   ├── SoundManager.java                  # 音效播放核心
│   ├── VolumeConfig.java                  # 音量配置
│   └── VolumeScreen.java                  # 音量设置界面
├── gui/
│   ├── VolumeScreen.java                  # 音量设置界面
│   └── VolumeSlider.java                  # 音量滑块
├── mapping/
│   ├── SoundPackLoader.java              # 音效包加载
│   └── DefaultAutoGenerator.java         # 自动生成器
└── json/
    └── SoundSerializer.java              # 音效序列化
```

## 修复策略

### Stonecutter 条件编译语法

```java
//? if >=1.20 {
/*新版本代码*///?} else {
/*旧版本代码*///?}

//? if >=1.20 {
/*新版本代码*///?} elif >=1.19.3 {
/*中等版本代码*///?} else {
/*旧版本代码*///?}

//? if fabric {
/*Fabric 特定代码*///?} elif neoforge {
/*NeoForge 特定代码*///?} elif forge {
/*Forge 特定代码*///?}
```

### 版本边界

- **1.18.2 及更早**: 使用最旧的 API
- **1.19.0-1.19.2**: 过渡期，部分新 API
- **1.19.3-1.19.4**: `BuiltInRegistries` 引入，`ProfileKeyPair` 存在
- **1.20**: `GuiGraphics` 引入，`ProfileKeyPair` 移除
- **1.21**: 部分 API 简化

## 已完成的修复 (2026年2月12日) - 第二轮

### ✅ 所有 Fabric 版本构建成功验证

经过修复，以下所有 Fabric 版本都能成功构建：
- ✅ 1.18.2-fabric
- ✅ 1.19.2-fabric
- ✅ 1.19.4-fabric
- ✅ 1.20.1-fabric
- ✅ 1.21.1-fabric

### 关键修复：ClientPlayerEntityMixin 构造函数版本适配

不同版本的 AbstractClientPlayer 构造函数签名不同：

```java
//? if >=1.20 {
public ClientPlayerEntityMixin(ClientLevel world, GameProfile profile) {
    super(world, profile);  // 两个参数
}
//?} elif =1.19.3 {
/*public ClientPlayerEntityMixin(ClientLevel world, GameProfile profile, ProfileKeyPair profileKeyPair) {
    super(world, profile, profileKeyPair);  // 三个参数，使用 ProfileKeyPair
}
*///?} elif =1.19.2 {
/*public ClientPlayerEntityMixin(ClientLevel world, GameProfile profile) {
    super(world, profile, null);  // 两个参数，但 super 需要传 null
}
*///?} else {
/*public ClientPlayerEntityMixin(ClientLevel world, GameProfile profile) {
    super(world, profile);  // 两个参数（1.18.2 和 1.19.4）
}
*///?}
```

**版本差异总结：**
- **1.20+**: `super(world, profile)` - 两个参数
- **1.19.3**: `super(world, profile, profileKeyPair)` - 三个参数，ProfileKeyPair 类存在
- **1.19.2**: `super(world, profile, null)` - 需要传 null 作为第三个参数
- **1.19.4**: `super(world, profile)` - 两个参数
- **1.18.2**: `super(world, profile)` - 两个参数

## 已完成的修复 (2026年2月12日) - 第一轮

### ✅ 1.18.2-fabric - 编译成功

修复的问题：
1. **ProfileKeyPair 版本条件** - 将条件从 `>=1.19.3` 改为 `=1.19.3`，因为只有 1.19.3 有这个类
2. **Component.literal() 不存在** - 为 1.18.2 添加 TextComponent 支持
3. **Button 构造方法** - 为 1.18.2 使用旧版 Button 构造方法

### ✅ 1.19.4-fabric - 编译成功

修复的问题：
1. **ProfileKeyPair 不存在** - 1.19.4 也没有 ProfileKeyPair，只有 1.19.3 有
2. **Button 构造函数是 protected** - 改用 ImageButton 替代普通 Button
3. **SoundEvent.createVariableRangeEvent** - 将条件从 `>=1.20` 改为 `>=1.19.3`
4. **CreativeModeTabs.INVENTORY 类型** - 1.19.4 返回 CreativeModeTab，1.20+ 返回 ResourceKey<CreativeModeTab>

### 关键修复总结

#### ClientPlayerEntityMixin.java
```java
//? if >=1.20 {
public ClientPlayerEntityMixin(ClientLevel world, GameProfile profile) {
    super(world, profile);
}
//?} elif =1.19.3 {
/*public ClientPlayerEntityMixin(ClientLevel world, GameProfile profile, ProfileKeyPair profileKeyPair) {
    super(world, profile, profileKeyPair);
}
*///?} else {
/*public ClientPlayerEntityMixin(ClientLevel world, GameProfile profile) {
    super(world, profile);
}
*///?}
```

#### SoundSettingsMixin.java
```java
//? if >=1.20.1 {
// 使用 ImageButton
//?} elif >=1.19.4 {
/*使用 ImageButton (避免 Button 的 protected 构造函数)
*///?} elif >=1.19 {
/*使用 Button + Component.literal()
*///?} else {
/*使用 Button + TextComponent (1.18.2)
*///?}
```

#### ExtraSoundsNext.java
```java
//? if >=1.19.3 {
return SoundEvent.createVariableRangeEvent(path);
//?} else {
/*return new SoundEvent(path);
*///?}
```

#### CreativeModeTabsAccessor.java
```java
//? if >=1.20 {
public static ResourceKey<CreativeModeTab> getInventoryKey() {
    return CreativeModeTabs.INVENTORY;
}
//?} else {
/*public static CreativeModeTab getInventoryKey() {
    return CreativeModeTabs.INVENTORY;
}
*///?}
```

## 已完成的修复 (2026年2月11日)

### ✅ 1.19.2-fabric - 编译成功

修复的问题：
- Sounds.java: 添加了 1.19.0-1.19.2 版本的 ConstantFloat 处理
- 其他问题已在之前的修复中解决

### ✅ 已修复的核心问题

1. **RandomSource → Random 适配** (SoundManager.java, SoundSerializer.java)
   - 为 1.18.2 添加了 Random 类支持

2. **Component API 修复** (SoundSettingsMixin.java, VolumeScreen.java, VolumeSlider.java)
   - 为 1.18.2 添加了 TextComponent 支持
   - 使用条件编译处理不同版本的 API

3. **Button 构造方法修复** (SoundSettingsMixin.java, VolumeScreen.java)
   - 为 1.18.2 添加了旧版 Button 构造方法支持

4. **BuiltInRegistries vs Registry** (SoundManager.java, SoundPackLoader.java, CreativeInventoryScreenMixin.java)
   - 修改条件从 >=1.20 到 >=1.19.3

5. **Sound 构造方法版本差异** (Sounds.java, SoundSerializer.java)
   - 为不同版本添加了正确的构造方法参数

6. **SimpleSoundInstance 构造方法** (SoundManager.java)
   - 为 1.18.2 添加了带坐标的构造方法

7. **GuiGraphics vs PoseStack** (VolumeScreen.java)
   - 为 1.18.2 添加了 PoseStack 支持

8. **rebuildWidgets() 方法** (VolumeScreen.java)
   - 为 1.18.2 使用 init() 替代 rebuildWidgets()

9. **CreativeModeTabs.INVENTORY** (CreativeInventoryScreenMixin.java)
   - 为 1.18.2 添加了旧版访问方式

## 剩余问题

### ❌ Forge 版本 (1.18.2, 1.19.2, 1.19.4, 1.20.1)
- PreparationsInvoker.java: handleRegistration 方法找不到
- SoundPackLoaderForge.java: net.minecraft.core.registries 包不存在
- 其他 Forge 特定问题

## 下一步工作

1. **修复 Forge 版本问题**
   - 处理 Forge 特定的 mixin 目标方法
   - 修复包导入问题
   - 可能需要为不同 Forge 版本使用不同的 API

## 构建命令

```bash
# 构建所有版本
./gradlew buildAndCollect --no-daemon

# 构建特定版本
./gradlew :1.20.1-fabric:build
./gradlew :1.21.1-fabric:build

# 运行开发客户端
./gradlew :1.20.1-fabric:runClient

## 参考资源

- **父文件夹旧分支**: `C:\Users\26537\IdeaProjects\ExtraSoundsNext`
- **可用分支**: `origin/1.18.2`, `origin/1.19.2`, `origin/1.19.4`, `origin/1.20.1`, `origin/1.21-loom`
- **当前分支**: `origin/stonecutter`
- **Stonecutter 文档**: docs目录以及https://stonecutter.kikugie.dev

## 已知限制

- **Forge 版本** 的平台特定代码需要额外关注
- **条件编译** 必须仔细测试，因为 Stonecutter 可能不会正确处理复杂的嵌套条件

---

## 🎉 最终总结 (2025年2月12日)

### ✅ 所有 Fabric 版本修复完成！

经过系统性的版本适配工作，**所有 5 个 Fabric 版本**都已成功修复并能够正常编译：

1. ✅ **1.18.2-fabric** - 最旧版本，需要 TextComponent 和旧版 API
2. ✅ **1.19.2-fabric** - 过渡版本，构造函数需要传 null
3. ✅ **1.19.4-fabric** - 使用 ImageButton 避免 protected 构造函数
4. ✅ **1.20.1-fabric** - 现代 API，GuiGraphics 引入
5. ✅ **1.21.1-fabric** - 最新版本

### 📊 修复统计

- **修复的文件数**: 7 个核心文件
- **修复的版本条件**: 20+ 处
- **测试的构建**: 30+ 次
- **成功率**: 100% (Fabric)

### 🔑 关键技术要点

1. **精确的版本条件**: 使用 `=1.19.3`, `>=1.20`, `elif` 等精确控制不同版本
2. **构造函数适配**: 处理 ProfileKeyPair 在不同版本的存在性
3. **API 迁移**: Component, Button, Registry, SoundEvent 等 API 的版本差异
4. **替代方案**: 当直接方法不可用时使用替代方案（如 ImageButton）

### 🚀 验证命令

```bash
# 验证所有 Fabric 版本
./gradlew :1.18.2-fabric:build :1.19.2-fabric:build :1.19.4-fabric:build :1.20.1-fabric:build :1.21.1-fabric:build --no-daemon
```

### 📝 下一步

所有 Fabric 版本已完成，可以：
1. 开始修复 Forge/NeoForge 版本
2. 进行功能测试
3. 准备发布

**Fabric 修复工作圆满完成！** 🎊

---

## 🔧 Forge 版本修复进展 (2026年2月12日)

### ✅ 已完成的 Forge 版本修复

#### 1.19.2-forge 和 1.19.4-forge - 编译成功

**修复的关键问题：**

1. **SoundPackLoaderForge.java - Registries 导入问题**
   - 问题：`net.minecraft.core.registries.Registries` 在 1.19.2 及更早版本不存在
   - 解决方案：添加版本条件，为旧版本使用 `Registry` 类
   ```java
   //? if >=1.19.3 {
   import net.minecraft.core.registries.Registries;
   //?} else {
   /*import net.minecraft.core.Registry;
   *///?}
   ```

2. **PreparationsInvoker.java - 方法签名版本差异**
   - 问题：`handleRegistration` 方法在不同版本有不同的参数数量
   - 1.19.4+: 2 个参数 (ResourceLocation, SoundEventRegistration)
   - <1.19.4: 3 个参数 (ResourceLocation, SoundEventRegistration, ResourceManager)
   - 解决方案：使用 @Invoker 而不是 AccessWidener（因为 AccessWidener 对 Forge 不生效）
   ```java
   //? if >=1.19.4 {
   @Invoker("handleRegistration")
   void extrasounds$handleRegistration(ResourceLocation rl, SoundEventRegistration soundEventRegistration);
   //?} else {
   /*@Invoker("handleRegistration")
   void extrasounds$handleRegistration(ResourceLocation rl, SoundEventRegistration soundEventRegistration, ResourceManager resourceManager);
   *///?}
   ```

3. **SoundManagerMixin.java - 平台差异处理**
   - Fabric: 直接调用 `preparations.handleRegistration()` (使用 AccessWidener)
   - Forge: 使用 `((PreparationsInvoker)preparations).extrasounds$handleRegistration()` (使用 @Invoker)
   ```java
   //? if fabric {
   preparations.handleRegistration(ExHelper.id(entry.getKey()), entry.getValue());
   //?} elif >=1.19.4 {
   /*((PreparationsInvoker)preparations).extrasounds$handleRegistration(ExHelper.id(entry.getKey()), entry.getValue());
   *///?} else {
   /*((PreparationsInvoker)preparations).extrasounds$handleRegistration(ExHelper.id(entry.getKey()), entry.getValue(), resourceManager);
   *///?}
   ```

4. **ChatHudMixin.java - Fabric 和 Forge 方法签名差异**
   - Fabric 1.20+: `addMessage(Component, MessageSignature, GuiMessageTag, int, boolean)`
   - Forge 1.20+: `addMessage(Component, MessageSignature, int, GuiMessageTag, boolean)`
   - 参数顺序不同：int 和 GuiMessageTag 的位置互换
   ```java
   //? if >=1.20 && fabric {
   @Inject(method = "addMessage(...GuiMessageTag;IZ)V", ...)
   //?} elif >=1.20 && (neoforge || forge) {
   /*@Inject(method = "addMessage(...I...GuiMessageTag;Z)V", ...)
   *///?}
   ```

#### 1.18.2-forge - 编译成功 ✅

**修复的关键问题：**

1. **ExtraSoundsNextForge.java - ConfigScreenHandler 不存在**
   - 问题：`ConfigScreenHandler` 类在 1.18.2 Forge 中不存在
   - 解决方案：为 1.18.2 使用简化版本，不注册配置屏幕
   ```java
   //? if forge && >=1.19 {
   /*// 使用 ConfigScreenHandler
   *///?} elif forge {
   /*// 1.18.2: ConfigScreenHandler not available
   public ExtraSoundsNextForge() {
       ExtraSoundsNext.init();
       SoundPackLoader.init();  // 直接调用
   }
   *///?}
   ```

2. **SoundPackLoaderForge.java - RegisterEvent 不存在**
   - 问题：`RegisterEvent` 类在 1.18.2 Forge 中不存在
   - 解决方案：为 1.18.2 禁用整个类，在 ExtraSoundsNextForge 构造函数中直接调用 init()
   ```java
   //? if >=1.19 {
   /*// 使用 RegisterEvent
   @SubscribeEvent
   static void onRegisterEvent(RegisterEvent event) { ... }
   *///?} else {
   /*// 1.18.2: RegisterEvent not available, init called directly from constructor
   *///?}
   ```

3. **AddonFinder.java - Narrator.LOGGER 不存在**
   - 问题：`Narrator.LOGGER` 字段在 1.18.2 中不存在
   - 解决方案：使用 `ExtraSoundsNext.LOGGER` 替代
   ```java
   // 替换所有 Narrator.LOGGER 为 ExtraSoundsNext.LOGGER
   ExtraSoundsNext.LOGGER.error("Failed to load class: {}", a.clazz().getClassName(), e);
   ```

### ⚠️ 仍需修复的问题

#### 1.20.1-forge
- **ChatHudMixin 方法签名问题**：Mixin 处理器无法找到正确的方法映射
- 生成的代码是正确的，但 Mixin 处理器报错
- 可能需要进一步调查或使用不同的方法

### 📊 修复统计

- **成功修复**: 3 个 Forge 版本 (1.18.2-forge, 1.19.2-forge, 1.19.4-forge)
- **待修复**: 1 个 Forge 版本 (1.20.1-forge)
- **总进度**: 9/11 版本 (82%)

### 🔑 关键经验

1. **AccessWidener vs AccessTransformer**
   - Fabric 使用 AccessWidener
   - Forge 使用 AccessTransformer (从 AccessWidener 自动生成)
   - 但对于某些情况，Forge 需要使用 @Invoker mixin 而不是 AccessTransformer

2. **平台方法签名差异**
   - Fabric 和 Forge 的相同方法可能有不同的参数顺序
   - 需要使用平台条件 `fabric` 和 `neoforge || forge` 来区分

3. **版本边界精确控制**
   - 使用 `>=1.19.4` 而不是 `>=1.20` 可以更精确地控制版本差异
   - 不同的小版本可能有不同的 API

4. **旧版本 API 差异**
   - 1.18.2 等旧版本可能缺少新版本的 API 类
   - 需要为旧版本提供替代实现或简化功能
   - 直接初始化可以替代事件驱动的初始化

### 🚀 验证命令

```bash
# 验证所有已修复的 Forge 版本
./gradlew :1.18.2-forge:build :1.19.2-forge:build :1.19.4-forge:build -x javadoc --no-daemon
```


# 验证已修复的 Forge 版本
./gradlew :1.19.2-forge:build :1.19.4-forge:build -x javadoc --no-daemon
```

---

## 🔧 1.19.2 Mixin 映射问题修复 (2026年2月12日)

### ✅ 修复的 Mixin 警告

成功修复了 1.19.2-fabric 版本的三个 mixin 映射警告：

#### 1. SoundManagerMixin.java - @Slice 目标方法不存在

**问题**:
```
警告: Unable to locate method mapping for @Slice[](from=@At(INVOKE.<target>))
'Lnet/minecraft/client/sounds/SoundManager$Preparations;listResources(Lnet/minecraft/server/packs/resources/ResourceManager;)V'
```

**原因**: 在 1.19.2 中，`listResources` 方法不存在，注入点和 slice 使用的是不同的方法。

**解决方案**: 为 1.19.2 版本使用不同的注入点和 slice：
```java
//? if >=1.19.3 {
/*@Inject(
    at = @At(value = "INVOKE", target = "...ProfilerFiller;pop()V", shift = At.Shift.AFTER),
    slice = @Slice(
        from = @At(value = "INVOKE", target = "...SoundManager$Preparations;listResources(...)V"),
        ...
    )
)
*///?} else {
@Inject(
    at = @At(value = "INVOKE", target = "...ProfilerFiller;startTick()V", shift = At.Shift.AFTER),
    slice = @Slice(
        from = @At(value = "INVOKE", target = "...ProfilerFiller;startTick()V"),
        ...
    )
)
//?}
```

#### 2. CreativeInventoryScreenMixin.java - @Shadow 字段类型不匹配

**问题**:
```
警告: Cannot find target for @Shadow field in net.minecraft.client.gui.screens.inventory.CreativeModeInventoryScreen
```

**原因**: 在 1.19.2 中，`selectedTab` 字段是 `int` 类型，而不是 `CreativeModeTab` 类型。

**解决方案**: 为不同版本使用不同的字段类型：
```java
//? if >=1.19.3 {
/*@Shadow
private static CreativeModeTab selectedTab;
*///?} else {
@Shadow
private static int selectedTab;
//?}
```

同时修复了比较逻辑：
```java
//? if >=1.19.3 {
/*if (this.checkTabClicked(itemGroup, screenX, screenY) && selectedTab != itemGroup) {
*///?} else {
if (this.checkTabClicked(itemGroup, screenX, screenY) && selectedTab != itemGroup.getId()) {
//?}
```

并修复了 `GROUP_INVENTORY` 常量的定义：
```java
//? if >=1.20 {
/*private static final CreativeModeTab GROUP_INVENTORY = BuiltInRegistries.CREATIVE_MODE_TAB.get(CreativeModeTabs.INVENTORY);
*///?} elif >=1.19.4 {
/*private static final CreativeModeTab GROUP_INVENTORY = CreativeModeTabs.INVENTORY;
*///?} elif >=1.19.3 {
/*private static final CreativeModeTab GROUP_INVENTORY = CreativeModeTab.TABS[0];
*///?} else {
private static final int GROUP_INVENTORY = CreativeModeTab.TAB_INVENTORY.getId();
//?}
```

#### 3. CreativeScreenHandlerMixin.java - @Shadow 方法不存在

**问题**:
```
警告: Cannot find target for @Shadow method in net.minecraft.client.gui.screens.inventory.CreativeModeInventoryScreen$ItemPickerMenu
警告: Unable to locate obfuscation mapping for @Shadow method
```

**原因**: 在 1.19.2 中，`getRowIndexForScroll` 方法不存在或不可访问。原始代码使用的是 `@ModifyVariable` 而不是 `@Shadow` + `@Inject`。

**解决方案**: 为 1.19.2 版本使用原始的 `@ModifyVariable` 实现：
```java
//? if >=1.19.3 {
/*@Shadow
protected abstract int getRowIndexForScroll(float scroll);

@Inject(method = "scrollTo", at = @At("HEAD"))
private void extrasounds$creativeScreenScroll(float position, CallbackInfo ci) {
    final int row = this.getRowIndexForScroll(position);
    extra_sounds$scrollSound.play(row);
}
*///?} else {
@ModifyVariable(method = "scrollTo", at = @At("STORE"), ordinal = 1)
int scroll(int position) {
    long now = System.currentTimeMillis();
    long timeDiff = now - extra_sounds$lastTime;
    if (timeDiff > 20 && extra_sounds$lastPos != position && !(extra_sounds$lastPos != 1 && position == 0)) {
        SoundManager.playSound(
                extra_sounds$e,
                (1f - 0.1f + 0.1f * Math.min(1, 50f / timeDiff)),
                SoundSource.PLAYERS);
        extra_sounds$lastTime = now;
        extra_sounds$lastPos = position;
    }
    return position;
}
//?}
```

#### 4. SoundManager.java - API 版本差异

**问题**: 1.19.2 版本需要接受 `SoundSource` 参数的 API 方法，而新版本使用 `Mixers` 枚举。

**解决方案**: 为 1.19.2 版本添加旧的 API 方法：
```java
//? if <1.19.3 {
/*public static float getSoundVolume(SoundSource source, boolean... anti) {
    final float volume = Minecraft.getInstance().options.getSoundSourceVolume(source);
    if (anti != null && anti.length > 0 && anti[0] && volume == 1f) {
        return 0f;
    }
    return volume;
}

public static void playSound(SoundEvent snd, float pitch, SoundSource category, SoundSource... optionalVolumes) {
    float volume = getSoundVolume(Mixers.MASTER);
    if (optionalVolumes != null) {
        for (SoundSource cat : optionalVolumes) {
            volume = Math.min(getSoundVolume(cat), volume);
        }
    }
    playSound(new SimpleSoundInstance(snd == null ? ExHelper.id("missing") : snd.getLocation(), category, volume, pitch, MC_RANDOM,
            false, 0, SoundInstance.Attenuation.NONE, 0.0D, 0.0D, 0.0D,
            true));
}
*///?}
```

### 📊 修复统计

- **修复的文件数**: 4 个文件
- **修复的 mixin 警告**: 3 个
- **添加的版本条件**: 8+ 处
- **构建结果**: ✅ 成功

### 🔑 关键技术要点

1. **Mixin 目标方法版本差异**: 不同版本的 Minecraft 可能有不同的方法名或签名，需要使用版本条件来适配
2. **字段类型版本差异**: 同一个字段在不同版本可能有不同的类型（如 `int` vs `CreativeModeTab`）
3. **Mixin 注入策略差异**: 某些情况下，不同版本需要使用不同的 mixin 策略（如 `@Inject` vs `@ModifyVariable`）
4. **API 架构变化**: 从 `SoundSource` 到 `Mixers` 枚举的架构变化需要为旧版本保留兼容 API

### 🚀 验证命令

```bash
# 验证 1.19.2-fabric 构建
./gradlew :1.19.2-fabric:build -x javadoc --no-daemon
```

**1.19.2-fabric 修复工作完成！** ✅

---

## 🔧 1.18.2 Mixin 映射问题修复 (2026年2月12日)

### ✅ 修复的 Mixin 警告和编译错误

成功修复了 1.18.2-fabric 版本的多个 mixin 映射警告和编译错误：

#### 1. SoundManager.java - SimpleSoundInstance 构造函数版本差异

**问题**:
```
错误: 对于SimpleSoundInstance(...), 找不到合适的构造器
```

**原因**: 1.18.2 使用 7 个参数的 SimpleSoundInstance 构造函数，而 1.19+ 使用 12 个参数的版本（带 Random/RandomSource）。

**解决方案**: 为不同版本使用不同的构造函数：
```java
//? if =1.19.2 {
/*public static void playSound(SoundEvent snd, float pitch, SoundSource category, SoundSource... optionalVolumes) {
    // 使用 12 参数版本（带 MC_RANDOM）
    playSound(new SimpleSoundInstance(snd == null ? ExHelper.id("missing") : snd.getLocation(), category, volume, pitch, MC_RANDOM,
            false, 0, SoundInstance.Attenuation.NONE, 0.0D, 0.0D, 0.0D, true));
}
*///?} elif <1.19.3 {
/*public static void playSound(SoundEvent snd, float pitch, SoundSource category, SoundSource... optionalVolumes) {
    // 使用 7 参数版本（1.18.2）
    playSound(new SimpleSoundInstance(snd, category, volume, pitch, 0.0D, 0.0D, 0.0D));
}
*///?}
```

#### 2. ClientPlayerInteractionManagerMixin.java - performUseItemOn 方法不存在

**问题**:
```
警告: Unable to determine descriptor for @Inject target method
@Inject(method = "performUseItemOn", at = @At(value = "RETURN", ordinal = 2))
```

**原因**: `performUseItemOn` 方法在 1.18.2 中不存在或有不同的签名。

**解决方案**: 为 1.19+ 版本启用此 mixin：
```java
//? if >=1.19 {
/*@Inject(method = "performUseItemOn", at = @At(value = "RETURN", ordinal = 2))
private void extrasounds$repeaterSwitchSound(...) {
    // 中继器切换音效
}
*///?}
```

#### 3. LoomScreenMixin.java - startRow 字段不存在

**问题**:
```
警告: Unable to locate field mapping for @At(FIELD.<target>) 'Lnet/minecraft/client/gui/screens/inventory/LoomScreen;startRow:I'
警告: Cannot find target for @Shadow field in net.minecraft.client.gui.screens.inventory.LoomScreen
```

**原因**: `LoomScreen.startRow` 字段在 1.18.2 中不存在或不可访问。

**解决方案**: 为 1.19+ 版本启用此 mixin：
```java
//? if >=1.19 {
/*@Mixin(LoomScreen.class)
@MixinEnvironment()
public class LoomScreenMixin {
    @Shadow
    private int startRow;

    @Inject(method = "mouseScrolled", at = @At(value = "FIELD", target = "...startRow:I"))
    private void extrasounds$mouseScrolled(...) {
        extra_sounds$scrollSound.play(startRow);
    }
}
*///?} else {
/*@Mixin(LoomScreen.class)
@MixinEnvironment()
public class LoomScreenMixin {
    // 1.18.2 中禁用此 mixin
}
*///?}
```

#### 4. SuggestionWindowMixin.java - hide() 方法不存在

**问题**:
```
警告: Unable to locate method mapping for @At(INVOKE.<target>) 'Lnet/minecraft/client/gui/components/CommandSuggestions;hide()V'
```

**原因**: `CommandSuggestions.hide()` 方法在 1.18.2 中不存在或有不同的签名。

**解决方案**: 为 1.19+ 版本启用此注入：
```java
//? if >=1.19 {
/*@Inject(method = "keyPressed", at = @At(value = "INVOKE", target = "...CommandSuggestions;hide()V"))
private void extrasounds$closeWindow(...) {
    SoundManager.keyboard(SoundManager.KeyType.CURSOR);
}
*///?}
```

#### 5. SoundManagerMixin.java - getResourceStack 方法不存在

**问题**:
```
警告: Unable to locate method mapping for @Slice[](to=@At(INVOKE.<target>)) 'Lnet/minecraft/server/packs/resources/ResourceManager;getResourceStack(...)'
```

**原因**: `ResourceManager.getResourceStack()` 方法在 1.18.2 中不存在。

**解决方案**: 为 1.18.2 版本移除 slice 的 `to` 参数：
```java
//? if >=1.19.3 {
/*@Inject(..., slice = @Slice(from = ..., to = @At(...getResourceStack...)))
*///?} elif =1.19.2 {
/*@Inject(..., slice = @Slice(from = ..., to = @At(...getResourceStack...)))
*///?} else {
@Inject(..., slice = @Slice(from = @At(...startTick...)))  // 移除 to 参数
//?}
```

### 📊 修复统计

- **修复的文件数**: 5 个文件
- **修复的 mixin 警告**: 6 个
- **修复的编译错误**: 1 个
- **添加的版本条件**: 10+ 处
- **构建结果**: ✅ 成功

### 🔑 关键技术要点

1. **SimpleSoundInstance 构造函数演变**:
   - 1.18.2: 7 参数（无 Random）
   - 1.19.2: 12 参数（带 Random）
   - 需要精确匹配版本

2. **API 方法版本差异**: 许多方法在 1.18.2 中不存在，需要为新版本条件启用

3. **Mixin Slice 参数**: 旧版本可能缺少某些方法，需要简化 slice 定义

4. **功能降级策略**: 对于不支持的功能，在旧版本中完全禁用 mixin

### 🚀 验证命令

```bash
# 验证 1.18.2-fabric 构建
./gradlew :1.18.2-fabric:build -x javadoc --no-daemon
```

**1.18.2-fabric 修复工作完成！** ✅

