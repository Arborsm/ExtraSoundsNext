{
  "schemaVersion": 1,
  "id": "${id}",
  "name": "${name}",
  "version": "${version}",
  "authors": ["${authors}"],
  "contributors": ["${contributors}"],
  "contact": {
    "sources": "${sources_url}",
    "issues": "${issues_url}",
    "homepage": "${homepage_url}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord_url}"
      }
    }
  },
  "description": "${description}",
  "icon": "assets/extrasounds/icon.png",
  "license": "${license}",
  "environment": "*",
  "accessWidener": "aw/${minecraft}.accesswidener",
  "entrypoints": {
    "main": [
      "dev.arbor.extrasoundsnext.platform.fabric.FabricEntrypoint"
    ],
    "client": [
      "dev.arbor.extrasoundsnext.platform.fabric.FabricClientEntrypoint"
    ]
  },
  "mixins": [
    "extrasounds.mixins.json"
  ]
}
